import { TSelectableColor } from '@/types';

const CATEGORY_MOCK: { id: number; title: string; color: TSelectableColor }[] =
  [{ id: 1, title: '테스트1', color: 'primary' }];

export { CATEGORY_MOCK };
